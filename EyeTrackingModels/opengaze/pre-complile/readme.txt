This is a pre-complied package for OpenGaze, including OpenFace and Caffe library.
To use the package, you first need to install several dependencies, such as Nvidia driver, cuda and cudnn.
To install, just run `sudo dpkg -i opengaze.deb`
